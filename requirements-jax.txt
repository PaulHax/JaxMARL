# Core JAX (CPU-only for development)
jax
jaxlib
chex
flax

# Training (algs extras from pyproject.toml)
optax
distrax
flashbax==0.1.0
wandb
hydra-core>=1.3.2
omegaconf>=2.3.0
tqdm>=4.66.0

# Testing
pytest
pytest-xdist

# Benchmarking
matplotlib
pandas

# CybORG from ITM-Kitware fork (for equivalence tests)
CybORG @ git+https://github.com/ITM-Kitware/cage-challenge-2.git@main#subdirectory=CybORG
